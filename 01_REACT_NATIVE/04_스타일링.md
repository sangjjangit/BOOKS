# 04_스타일링


## 4.1 스타일링
- 인라인 스타일링: 컴포넌트에 직접 스타일을 입력하는 방식
- 클래스 스타일링: 컴포넌트에 직접 x, 스타일시트에 정의된 스타일을 사용
- 여러 개의 스타일 적용시, 배열을 이용
- 외부 스타일 이용하기, `styles/4/style.tsx`

## 4.2 리액트 네이티브 스타일
- flex와 범위
- flex
    - 값은 숫자로 받는다.
    - 값이 0 일때는 설정된 width, height 값에 따라 크기 결정
    - 양수인 경우 flex값에 비례하여 크기가 조정

- 정렬
    - flexDirection
    - column: 세로 정렬(기본값)
    - column-reverse: 세로 역순 정렬
    - row: 가로 정렬
    - row-reverse: 가로 역순 정렬
    - justifyContent: flexDirection에서 결정한 방향과 동일한 방향으로 정렬
    - alignItems: flexDirection에서 결정한 방향과 수직인 방향으로 정렬
    - flexDirection이 row 일 때, justifyContent의 값에 따라 정렬하는 모습
    - flex-start: 시작점부터 정렬(기본값)
    - flex-end: 끝에서부터 정렬
    - center: 중앙 절렬
    - space-between: 컴포넌트 사이의 공간을 동일하게 정렬
    - space-around: 컴포넌트 각각의 주변 공간을 동일하게 정렬
    - space-evenly: 컴포넌트 사이와 양 끝에 동일한 공간으로 정렬
    - flexDirection이 row 일 때, alignItems의 값에 따라 정렬하는 모습
    - flex-start: 시작점부터 정렬(기본값)
    - flex-end: 끝에서부터 정렬
    - center: 중앙 절렬
    - stretch: alignItems의 방향으로 컴포넌트 확장(공간채움)
    - baseline: 컴포넌트 내부의 텍스트(text) 베이스라인(baseline)을 기준으로 정렬

- 그림자
    - shadowColor: 그림자 색 설정
    - shadowOffset: width, height 그림자 거리 설정
    - shadowOpacity: 그림자의 불투명도 설정
    - shadownRadius: 그림자의 흐름 반경 설정

## 4.3 스타일드 컴포넌트
- https://styled-components.com/
- npm install styled-components
- 완성된 스타일을 그대로 상속받아서 사용. styled(컴포넌트 이름)
- props 사용
- attrs 사용: 스타일드 컴포넌트에서 속성을 설정할 때 사용
- ThemeProvider
    - theme를 지정, 하나의 파일에서 미리 정의해둔 색을 하위 컴포넌트에서 사용